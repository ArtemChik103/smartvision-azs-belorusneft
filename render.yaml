services:
  - type: web
    name: smartvision-azs
    env: python
    buildCommand: pip install -r requirements.txt && python tools/video_generator.py
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.13
